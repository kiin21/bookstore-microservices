spring:
  application:
    name: order-service
  config:
    import: optional:configserver:http://localhost:8888/

server:
  port: 8082
  shutdown: graceful

---
spring:
  application:
    name: order-service
  config:
    activate:
      on-profile: docker
    import: configserver:http://config-server:8888

server:
  port: 8082
  shutdown: graceful