spring.application.name=config-server

# Server port
server.port=8888

# Use native profile for local file system
spring.profiles.active=native

# Search locations for configuration files
spring.cloud.config.server.native.search-locations=classpath:/config,file:./config

# Enable all endpoints for actuator (optional, useful for debugging)
management.endpoints.web.exposure.include=*
