server:
  port: 8888
    
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kiin21/bookstore-microservices-config
          default-label: main

management:
  endpoints:
    web:
      exposure:
        include: "*"

# Logging
logging:
  level:
    org.springframework.cloud.config: DEBUG